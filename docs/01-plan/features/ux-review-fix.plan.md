# Plan: UX 검토 및 수정

## 기본 정보

| 항목 | 내용 |
|------|------|
| **Feature Name** | ux-review-fix |
| **작성일** | 2026-02-11 |
| **작성자** | Claude Opus 4.5 |
| **상태** | Draft |

---

## 1. 배경 및 목적

전체 페이지별 UX 검토 결과, 일부 버튼/링크 연결 문제 및 미구현 기능이 발견되었습니다.
사용자 경험 개선을 위해 이러한 문제점들을 수정합니다.

---

## 2. 발견된 UX 문제점

### 2.1 Critical (즉시 수정 필요) ❌

| 페이지 | 문제점 | 위치 | 영향 |
|--------|--------|------|------|
| 대시보드 | "자세히 보기" 버튼 클릭 시 동작 없음 | `page.tsx:455-457` | 긴급 알림 확인 불가 |

### 2.2 Medium (기능 미완성) ⚠️

| 페이지 | 문제점 | 위치 | 영향 |
|--------|--------|------|------|
| 설정 | 저장 버튼 API 미연동 | `settings/page.tsx` | 설정 저장 불가 |
| 설정 | 비밀번호 변경 비활성화 | `settings/page.tsx` | 보안 기능 사용 불가 |
| 설정 | 다크 모드 비활성화 | `settings/page.tsx` | 테마 변경 불가 |

### 2.3 완료 확인 ✅

| 페이지 | 항목 | 상태 |
|--------|------|------|
| 사이드바 | accountId 기반 메뉴 표시 | ✅ 수정 완료 |
| 소재 분석 | "인사이트 및 전략 보기" 버튼 | ✅ 정상 작동 |
| 소재 상세 | "인사이트 생성" 버튼 | ✅ 정상 작동 |
| 소재 상세 | "전략 생성" 버튼 | ✅ 정상 작동 |
| 대시보드 | RecentInsights "전체 보기" 링크 | ✅ 정상 작동 |
| 대시보드 | PendingStrategies "전체 보기" 링크 | ✅ 정상 작동 |
| 대시보드 | 전략 수락/거절 버튼 | ✅ 정상 작동 |
| AI 인사이트 | 인사이트 생성 버튼 | ✅ 정상 작동 |
| AI 전략 | 전략 생성 버튼 | ✅ 정상 작동 |
| 리포트 | 리포트 생성/다운로드 | ✅ 정상 작동 |
| 알림 | 모두 읽음 처리 | ✅ 정상 작동 |

---

## 3. 수정 계획

### 3.1 Phase 1: Critical 수정 (즉시)

#### Task 1: 대시보드 "자세히 보기" 버튼 수정

**현재 코드** (`src/app/(dashboard)/accounts/[accountId]/page.tsx:455-457`):
```tsx
<Button variant="destructive" size="sm">
  자세히 보기
</Button>
```

**수정 계획**:
```tsx
<Button
  variant="destructive"
  size="sm"
  onClick={() => router.push(`/accounts/${accountId}/insights?severity=CRITICAL`)}
>
  자세히 보기
</Button>
```

또는 Link 컴포넌트 사용:
```tsx
<Link href={`/accounts/${accountId}/insights?severity=CRITICAL`}>
  <Button variant="destructive" size="sm">
    자세히 보기
  </Button>
</Link>
```

---

### 3.2 Phase 2: Medium 수정 (선택적)

#### Task 2: 설정 페이지 API 연동 (선택)

**범위**:
- 프로필 저장 API 연동
- 알림 설정 저장 API 연동

**참고**: 현재 인증 시스템이 mock 상태이므로, 실제 사용자 시스템 구축 후 진행

---

## 4. 전체 페이지-컴포넌트 연결 맵

```
홈 (/)
└─ [Get Started] → /accounts ✅

/accounts (계정 목록)
├─ [계정 연동] → /api/auth/tiktok ✅
└─ [계정 카드 클릭] → /accounts/{id} ✅

/accounts/{id} (대시보드)
├─ [새로고침] → API refetch ✅
├─ [날짜 선택] → URL param 업데이트 ✅
├─ [이전 기간 비교] → 차트 토글 ✅
├─ [긴급 알림 - 자세히 보기] → ❌ 미연결 (수정 필요)
├─ [최근 인사이트 - 전체 보기] → /accounts/{id}/insights ✅
├─ [대기 전략 - 전체 보기] → /accounts/{id}/strategies ✅
└─ [전략 수락/거절] → API 호출 ✅

/accounts/{id}/creatives (소재 분석)
├─ [소재 동기화] → POST API ✅
├─ [정렬/뷰 모드] → 상태 업데이트 ✅
└─ [인사이트 및 전략 보기] → /accounts/{id}/creatives/{creativeId} ✅

/accounts/{id}/creatives/{creativeId} (소재 상세)
├─ [돌아가기] → router.back() ✅
├─ [인사이트 생성] → POST API ✅
├─ [전략 생성] → POST API ✅
└─ [전략 수락/거절] → POST API ✅

/accounts/{id}/insights (AI 인사이트)
├─ [인사이트 생성] → POST API ✅
├─ [필터] → URL param ✅
└─ [읽음 처리] → PUT API ✅

/accounts/{id}/strategies (AI 전략)
├─ [전략 생성] → POST API ✅
├─ [필터/탭] → URL param ✅
└─ [수락/거절/시작/완료] → PUT API ✅

/accounts/{id}/reports (리포트)
├─ [리포트 생성] → POST API ✅
├─ [다운로드] → GET API ✅
└─ [삭제] → DELETE API ✅

/settings (설정)
├─ [저장] → ⚠️ API 미연동
├─ [비밀번호 변경] → ⚠️ 비활성화
└─ [다크 모드] → ⚠️ 비활성화

/notifications (알림)
├─ [모두 읽음] → PUT API ✅
├─ [보기] → 링크 이동 ✅
└─ [삭제] → DELETE API ✅
```

---

## 5. 우선순위

| 우선순위 | 항목 | 예상 작업 시간 |
|:--------:|------|:-------------:|
| **P0** | 대시보드 "자세히 보기" 버튼 연결 | 10분 |
| P2 | 설정 페이지 API 연동 | 2-4시간 |
| P3 | 다크 모드 구현 | 4-8시간 |

---

## 6. 체크리스트

- [x] 사이드바 accountId 기반 메뉴 표시 (완료)
- [x] 대시보드 긴급 알림 "자세히 보기" 버튼 연결 (완료)
- [ ] (선택) 설정 페이지 API 연동
- [ ] (선택) 다크 모드 구현

---

*Generated by bkit PDCA Skill v1.5.0*
